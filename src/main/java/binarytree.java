/*
import java.util.*;
import java.io.*;

public class binarytree {
    static int currentDepth;
    psvm throws IO exception


    File file = new File("primes4.txt");
    Scanner infile = new Scanner(file);
    Scanner infile = new Scanner File

    Node toot - new node infile.nextINt

    int maxdepth = 0, sumdepth

    while infile.has next int

    current depthe = 0
    insert root, infile.nextint()
    if current depthe > maxdepth
    maxdepthe = currentdepth;
    sumdepth += currentdepth


    averahe is going to be sumdepth divided by million numbers

    sout root, 998



    Node Root = new Node(infile.nextInt()); //read first number and create first node

    while(infile.hasNextInt())  //
    {
        Insert(Root, infile.nextInt());
    }
    System.out.println(Locate(Root, 998));

}

to create a depth make a class variable
int depth

 */

